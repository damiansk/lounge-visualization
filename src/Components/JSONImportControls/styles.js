export const styles = theme => ({
  button: {
    margin: theme.spacing.unit,
  },
  fileInput: {
    border: 0,
    clip: 'rect(0, 0, 0, 0)',
    height: '1px',
    overflow: 'hidden',
    padding: 0,
    position: 'absolute',
    whiteSpace: 'nowrap',
    width: '1px',
  },
});
